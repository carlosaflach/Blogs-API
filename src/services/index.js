const login = require('./loginService');
const user = require('./userService');
const category = require('./categoryService');
const post = require('./postService.js');

module.exports = {
  login,
  user,
  category,
  post,
};